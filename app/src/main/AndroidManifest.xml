<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rifat.rajshahitransportmanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AirPrice"></activity>
        <activity android:name=".AirSchedule" />
        <activity android:name=".AirMenu" />
        <activity android:name=".TrainPrice" />
        <activity android:name=".TrainIntercity" />
        <activity android:name=".TrainSchedule" />
        <activity android:name=".TrainMenu" />
        <activity android:name=".Emergency" />
        <activity android:name=".BusMenu" />
        <activity android:name=".MenuActivity" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="fullSensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>